---
agent: speckit.constitution
---

# Additional Mandates
- After every code change, run `xcodebuild -scheme SilentX -configuration Debug -destination 'platform=macOS' build` and ensure it succeeds before delivering.
